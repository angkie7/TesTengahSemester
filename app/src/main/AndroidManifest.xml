<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testengahsemester">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Model.RecyclerLihatKelas"></activity>
        <activity android:name=".Model.AdaLayarUtama" />
        <activity android:name=".Model.DosenUtama" />
        <activity android:name=".Model.DataDosen" />
        <activity android:name=".Model.CreateMatkul" />
        <activity android:name=".Model.CreateMahasiswa" />
        <activity android:name=".Model.CreateKrs" />
        <activity android:name=".Model.CreateDosen" />
        <activity android:name=".Model.DataAdmin" />
        <activity android:name=".Model.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>